import { StyleSheet } from "react-native";

export const OPENSANS_REGULAR = "SpaceMono-Regular";
export const globalStyles = StyleSheet.create({
    globalFont: {
        fontFamily: OPENSANS_REGULAR
    }
})